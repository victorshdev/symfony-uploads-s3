<form method="post" action="{{ path('app_banner_delete', {'id': banner.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ banner.id) }}">
    <button class="btn">Delete</button>
</form>
